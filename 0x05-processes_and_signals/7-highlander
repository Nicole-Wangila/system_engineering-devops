#!/usr/bin/env bash
# Handle SIGTERM: print message and exit so the test harness can finish

trap 'echo "I am invincible!!!" >&2; exit 0' SIGTERM

while true
do
    echo "To infinity and beyond"
    sleep 2
done
