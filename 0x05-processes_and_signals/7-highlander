#!/usr/bin/env bash
# A Bash script that runs indefinitely and catches the SIGTERM signal without exiting

handle_sigterm() {
    echo "I am invincible!!!"
}

trap 'handle_sigterm' SIGTERM

while true; do
    echo "To infinity and beyond"
    sleep 2
done
